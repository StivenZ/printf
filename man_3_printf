./" Manual page for _printf function
.TH man 3 "July 21st, 2020" "0.1" "_printf man page"
.SH NAME
.B _printf

.SH SYNOPSIS
#include "holberton.h"

int _printf(const char *
.I format
, ...);

.B _printf
.I FORMAT

.SH DESCRIPTION
The _printf function print argument(s) according to holberton
.I format.

This function writes a string with options that are converted to the corresponding type,
depending on the characters following the character '%'.

Prints any parameters assigned to it onto the output screen.

The function returns the count of the characters printed.

.SH OPTIONS
- %s prints a string.

- %c prints a single character according to the ascii code.

- %d prints a decimal (base 10) number.

- %i prints an integer in base 10.

.SH COPYRIGHT
.B We supose that Holberton takes care of it.

.SH AUTHOR(S)
Written by
.B Stiven Espinosa Zapata
and
.B Santiago Zapata Bedoya.